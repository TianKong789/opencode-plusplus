# Benchmarks

> Benchmark definitions and result tracking.

## Structure

- Per-skill benchmark files go in this directory.
- Each benchmark defines input, expected output, and timeout.
- Results are tracked by `BenchmarkRunner`.
